/**
 * RangeBites — example config.
 * Target project: Amplitude **855341** (snake_case events; North Star = nav_handoff).
 * Copy to config.js. Paste the Browser SDK API key FROM project 855341 only.
 * Leave key empty to keep analytics session-queued only (no network).
 */
window.FOOD_RADAR_AMPLITUDE_PROJECT_ID = "855341";
window.FOOD_RADAR_AMPLITUDE_API_KEY = ""; // paste Browser API key from project 855341
/* Optional debug: log event NAMES only (never props) */
/* window.FOOD_RADAR_ANALYTICS_DEBUG = true; */
